1. Install:

   helm upgrade --install weather-backend-server . \
     --namespace skycast --create-namespace \
     --values values-<env>.yaml

2. Verify:

   kubectl get pods -l app.kubernetes.io/instance=weather-backend-server
   kubectl get svc -l app.kubernetes.io/instance=weather-backend-server
